export const Header = () => {
  // i want to on click send search data to the app.tsx and fetch data there

  return (
    <header>
      <h1>Pokemon cards</h1>
      <h3>Use input to add more cards</h3>
    </header>
  );
};
